# Changelog

All notable changes to this project will be documented in this file.

This project follows Semantic Versioning.

## Unreleased

## v0.1.0
- Core inference-native primitives: workflow, execution, artifact (URI-first)
- Multi-deployment scheduling with routing policy (weighted/sticky/failover)
- Delivery modes: SYNC / ASYNC(callback) / STREAM(SSE)
- Plugin system: executor, artifact, callback, output, observability
- Runnable examples with mock external APIs
- E2E test suite and CI gates